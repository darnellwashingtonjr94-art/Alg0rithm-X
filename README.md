# # Alg0rithm-X

A multi-disciplinary runtime environment spanning chaotic human dynamic modeling, hybrid quantum-classical optimization, abstract syntax tree (AST) metaprogramming, and computational visual cloaking.

## ⚙️ Subsystem Architecture

1. **AI Human Dynamics (`/ai_human_dynamics`)**
   * Computes non-linear hedonic adaptation and N-Depth recursive Theory of Mind (ToM) matrix projections. 
   * Validates inputs against Teleological (goal-seeking) and Deontological (hard-boundary) filters.

2. **Visual Cloak (`/visual_cloak`)**
   * Real-time OpenCV pipeline utilizing Gaussian Mixture Models (MOG2) and Farneback dense optical flow.
   * Calculates homographic matrices via ORB feature matching to dynamically align background frames.

3. **C# Obfuscator (`/obfuscator_cs`)**
   * Interfaces with headless Roslyn analyzers to dump C# ASTs.
   * Flattens linear execution structures into switch-case state dispatchers using opaque predicates and encrypts string payloads at compile-time via AES-256 (Rijndael).

4. **Quantum Solver (`/quantum_solver`)**
   * Classical-Quantum hybrid Variational Quantum Eigensolver (VQE).
   * HPC Cluster bridge designed to split optimization parameters across multiprocessing pools.

## 🚀 Execution Guide

Ensure all dependencies from `requirements.txt` are installed. Run the primary gateway to initialize the ThreadPoolExecutor:

```bash
python src/main.py --mode full
